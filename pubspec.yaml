name: three_d_pie_chart
description: A customizable 3D tilted ring chart for Flutter with smooth animations, glow effects, theme adaptation, and 30+ style parameters.
version: 1.0.3
homepage: https://github.com/akanshujamwal/three_d_pie_chart
repository: https://github.com/akanshujamwal/three_d_pie_chart
issue_tracker: https://github.com/akanshujamwal/three_d_pie_chart/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
