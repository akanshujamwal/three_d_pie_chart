name: three_d_pie_chart_example
description: A customizable 3D tilted ring chart for Flutter with smooth animations, glow effects, theme adaptation, and 30+ style parameters.
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  three_d_pie_chart:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
